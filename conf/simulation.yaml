simulation:
  model_file: /home/dingeldein/Desktop/p4p6_pdb_models_dec_25/models_set0.pt
  n_pixels: 128
  pixel_size: 1.5           # Angstrom per pixel
  sigma: [2.0, 2.0]         # Gaussian width [min, max] in Angstrom
  shift: 15.0               # Max in-plane shift in Angstrom
  defocus: [1.5, 3.5]       # CTF defocus [min, max] in micrometers
  snr: [0.5, 0.5]           # SNR [min, max] (log10 scale)
  amp: 0.1                  # Amplitude contrast
  b_factor: [1.0, 100.0]
  n_bg_min: 0
  n_bg_max: 10
  padding_factor: 2
  exclusion_radius: 0.0
  max_placement_attempts: 1000
