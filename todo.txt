restructuring the codebase:
- configs
- src/cryo_sbi 
    - simulator
    - utils 
    - training (training and inference scripts)
    - models
- tests
- remove tutorial

other todos:
    - refactor the config files (also keep everything lower case)
        - one main config which includes the others (training + model, simulator)
    - inference script should take take config file as well, not just a checkpoint path. 
        - only data path and model weight path should be given as an argument, everything else should be in the config file (model architecture, hyperparameters, etc.)
    - tensorboard should write into hydra log dir
    - tensorboard should log hyperparameters as well
    - training should be with hydra directly
    - simulator should work with OmegaConf as well, so i can give it a config file (CryoEMSimulator(config=sim.yaml) for example)
    - both simulator classes in one file
    - one big training function instead of two separate ones for the two simulators
    - add hook to run training and inference from command line with hydra (so i can do `python -m training --config-name=training_config.yaml train.epochs` for example)
    - no loss file needed anymore because everything is in the tensorboard logs, so remove that and the corresponding code

My general goal is to clean uo the repository